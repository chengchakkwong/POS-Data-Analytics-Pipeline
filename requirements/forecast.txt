# Official forecasting runtime for the v3 analytics pipeline.
# Install after requirements/prod.txt when target-stock X/Y forecasting is needed.
#
#   pip install -r requirements/forecast.txt
#
# Production Cloud / scheduled jobs should use this Prophet stack.
# NeuralProphet stays in requirements/neuralprophet.txt for experiments only.

-r prod.txt

joblib>=1.3,<2
tqdm>=4.66,<5
prophet>=1.1,<2
