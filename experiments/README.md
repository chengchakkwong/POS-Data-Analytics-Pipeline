# Experiments

Ad-hoc / research scripts that are **not** part of the supported `pos_pipeline` CLI.

| File | Notes |
|------|--------|
| `feature_engineering.py` | Feature experiments |
| `weather_engineering.py` | Weather feature experiments |

Do not schedule these in production. Prefer `python -m pos_pipeline.cli daily|analytics` for supported jobs.
