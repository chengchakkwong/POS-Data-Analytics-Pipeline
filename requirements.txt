# Production runtime: POS SQL sync + ABC/XYZ analytics + Firebase sync.
# Use requirements-demo.txt for the public offline demo.
# Install requirements-forecast.txt only when running inventory_forecast.py.

-r requirements-demo.txt

# SQL Server ETL
sqlalchemy==2.0.45
pyodbc==5.3.0
python-dotenv==1.2.1

# Firestore upload used by POS_Sync_Tool.py and classification sync.
firebase-admin>=6.5.0,<7
