# Production runtime: POS SQL sync + ABC/XYZ analytics + Firebase sync.
# Use requirements-demo.txt for the public offline demo.
# Install requirements-forecast.txt when running the analytics pipeline with Prophet.
-r requirements-demo.txt

# SQL Server ETL
sqlalchemy==2.0.45
pyodbc==5.3.0
python-dotenv==1.2.1

# Firestore upload used by pos_pipeline.cli (daily / analytics / min-multiple).
firebase-admin>=6.5.0,<7
